// Site settings you may want to change often. Edit a value here, save, and push; nothing else needs editing.
// Loaded before the other scripts on index.html.
const SITE = {
  // Job search: true shows "Open to …" in the hero and the About card, with a pulsing green dot.
  // false hides it, and the hero dot turns grey and stops pulsing.
  openToRoles: true,
  roles: 'Android roles',            // what you're open to, e.g. 'Android & mobile roles'
  jobLocations: 'Sydney or remote',  // where you're looking, e.g. 'Sydney, Melbourne or remote'

  // Where you live: shown in the hero, and used for the local time in the About card
  homeLocation: 'Sydney, Australia',
  homeCity: 'Sydney',
  timeZone: 'Australia/Sydney',      // IANA time-zone name, e.g. 'Australia/Melbourne'
};
